import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  circle: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  bgCirleContainer: {
    position: 'relative',
    alignItems: 'center',
    justifyContent: 'center',
  },
  absoluteCenter: {
    position: 'absolute',
    zIndex: 0,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
